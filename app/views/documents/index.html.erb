<p id="notice"><%= notice %></p>

<h1>Documents</h1>

<table>
  <thead>
    <tr>
      <th>Document</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @documents.each do |document| %>
      <tr>
        <td><%= document.document.original_filename %></td>
<!--        <td><%#= link_to 'Ver', document_path(document.id) %></td>-->
        <td><%= link_to 'Download', document.document.url %></td>
        <td><%= link_to 'Remover', document, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
